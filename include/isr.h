/*
 * 模块：算法周期调度
 * 设计目的：提供一次控制周期的纯算法调度入口，按命令位运行 PLL、开环或电流环。
 * 设计原理：先检查配置合法性，再依次执行同步算法和当前模式对应的控制算法。
 * 设计要点：本模块命名为 ISR 但只表示周期调用语义，不依赖平台入口或寄存器访问。
 * 输入：CONTROL_ALGO 总对象中的命令字、配置、输入采样量和内部状态。
 * 输出：更新后的 PLL 状态、控制输出、状态字和故障字。
 * 输出给谁用：应用层周期任务调用本模块，并读取 OUTPUT 交给外部执行流程。
 */
#ifndef ISR_H_
#define ISR_H_

#include "types.h"

void run_isr(CONTROL_ALGO *obj);

#endif
